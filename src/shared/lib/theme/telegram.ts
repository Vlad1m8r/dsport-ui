// Telegram colors not used: приложение использует только локальные темы light/dark.

export const applyTelegramTheme = (): void => {
  // no-op
};

export const bindTelegramThemeEvents = (): (() => void) => {
  return () => undefined;
};
